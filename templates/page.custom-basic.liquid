<div class="page-width custom-section-below">
  <header class="section-header text-center">
    {% comment %}<h1 class="section-header__title h2">{{ page.title }}</h1>{% endcomment %}
  </header>
  <div class="grid">
    <div class="grid__item medium-up--four-fifths medium-up--push-one-tenth">
      <div class="rte">
        {{ page.content }}
      </div>
    </div>
  </div>
  {% include 'dynamic-buy-button' %}
</div>